<UserControl x:ClassModifier="internal" x:Class="Egs.ZkooTutorial.TutorialLargeCircleAreaButtonUserControl"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:hostAppCoreViews="clr-namespace:Egs.Views;assembly=EgsHostAppCore"
        xmlns:local="clr-namespace:Egs.ZkooTutorial" mc:Ignorable="d" Background="Transparent">
    <Button x:Name="thisButton" Command="{Binding Command}" Style="{DynamicResource TransparentBackgroundButtonStyle}">
        <!--<Image Source="{Binding ImageSource}" />-->
        <local:OpaqueClickableImage Source="{Binding ImageSource}" />
    </Button>
</UserControl>
